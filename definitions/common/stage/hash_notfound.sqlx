config { type: "table" }
 
select count(data_hash) as insert from ${ref("asc_tgt003_patient_dimension")}
where data_hash not in 
(select data_hash from ${ref("asc_tgt003_patient_dimension_scd")})
